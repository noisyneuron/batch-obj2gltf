1. `git clone https://github.com/noisyneuron/batch-obj2gltf.git`
2. `cd batch-obj2gltf`
3. `npm i`
4. `npm install -g .`
5. Use it anywhere `obj2gltf-batch --input="INPUT_DIRECTORY" --output="OUTPUT_DIRECTORY"`
